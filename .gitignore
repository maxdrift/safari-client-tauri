# Same as main — dev artifacts left on disk when switching branches
.DS_Store
node_modules
/build
src-tauri/target
/.svelte-kit
/package
.env
.env.*
!.env.example
src-tauri/.updater/signing.key
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Full tree from main checkout (not tracked on gh-pages)
/src-tauri/
